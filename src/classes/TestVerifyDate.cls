@isTest
public class TestVerifyDate {  
    public static testmethod void tmethod()
        {
            verifydate.CheckDates(system.today(),system.today()+25);    	
            verifydate.CheckDates(system.today(),system.today()+30);
       }  
}